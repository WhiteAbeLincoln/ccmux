_continuation of [event-log-formats](../event-log-formats/prompt.md)_

> I want to support incremental / streaming updates as well for when I'm wrapping the agent cli (i.e. for Claude through the headless -p mode). This way we can display partial responses as the agent generates them instead of having to buffer the full message (and thus have the appearance of high latency). Similarly to the log format research, find out how each agent represents this type of communication between the local harness and the remote servers. Write the results to a new folder under research
